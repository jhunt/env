#!/bin/bash -e

(
cd ~/env/dot
for FILE in *; do
	cp $FILE ~/.$FILE
done
)

~/env/git-aliases

if [[ ! -f ~/.bashrc ]]; then
	touch ~/.bashrc
fi
grep -q "source $HOME/env/bashrc" ~/.bashrc || echo "source $HOME/env/bashrc" >> ~/.bashrc
source ~/.bashrc
